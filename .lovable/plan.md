Fix the certification flow so the name field is visible immediately when the member opens Level 1 Cert.

Plan:
1. Restore the Level 1 Certification button/card behavior so it opens the certificate name-entry modal directly instead of only showing the checklist first.
2. Keep the checklist/requirements available inside the modal, but make the primary action obvious: enter name and generate certificate when requirements are met.
3. Update the existing certified state button from “Edit Name” to clearer wording tied to generating a certificate with a name.
4. Verify in the live preview on the current course route that clicking Level 1 Cert shows the actual name input field, not just the course list/checklist.