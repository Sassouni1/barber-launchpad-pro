

# Rename "Get In Touch" → "How Can We Help?"

## Change
Update the card title in `src/components/dashboard/ContactSection.tsx` from "Get In Touch" to "How Can We Help?"

### File: `src/components/dashboard/ContactSection.tsx`
- Line ~68: Change `CardTitle` text from `"Get In Touch"` to `"How Can We Help?"`

